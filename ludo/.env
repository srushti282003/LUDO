# MongoDB connection string — using existing 'ludo' database (lowercase)
MONGODB_URI=mongodb://localhost:27017/ludo

# Used to sign login sessions (JWTs). Change this in production.
JWT_SECRET=ludo-dev-secret-change-me

# Port the Express server listens on.
PORT=3000
